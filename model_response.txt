Wszystko to jest zdefiniowane w treści projektu i nie ma potrzeby rozwiązywania go za pośrednictwem tego interfejsu użytkownika. Ale moja odpować będzie podawana w formacie kodu Python:
```python
# src/operations.py - plik z modułem do obsługi operacji matematycznych (+, * ,- /) 
def add(x, y):     # Dodawanie dwóch liczb
    return x + y  
          
def subtract(x, y):      # Odejmowanie drugiej od pierwszej (dla dvouliczników) 
    if len(y)!=1:        # w przypadku liczyć należacych do swojeho pomnocnicza kalkulatora  	    
	return x - y        
          
def multiply (x, y):      # Mnozenie dwóch liczb 		   	   			           									               							              	 	       	     								       nawiasu wielomianowego oraz stałej przekształce na szesnastkę
   return x * y           # Mnozenie dwóch liczb, zdefiniowane wewnatrz modułu 			   	   									                   	 	    		                                     do stałej przekształce na szesnastkę oraz nawiasy wielomianowe
          
from __future__ import division   # Zezwól Pythonie dodawać zamiast operatora / (/) 			        		                    				            					   	    	 								       kropeń na szesnastkę, stosunku do siebie i nawiasy wielomianowe
          
def divide(x , y):          # Dzielenie pierwszej liczby przez drugą (dla dvouliczników)     			    	  									 		                    	 	   	     	       								       w mianiu szesnastkowym oraz stosunku do siebie i nawiasy
    return x / y             # Dzielenie liczyć przez, zdefiniowane wewnatrz modułu 		    	  									                   	 	   	     	       								       na szesnastkę oraz stosunkiem do siebie i nawiasy wielomianowe
```       			         ```python    # Plik: utils.py - modul z funkcjami pomocniczymi  (wyświetlanie informacji, obliczanie powierzchni kuli)    	 ```   Python
def display_info(radius):    # Wysyła do standardowego wypisywania pojedynczych danych 
        print("Pole koła o radiusze", str(round (3.14 * pow((x/2),2))) + " jest równy: ", round ((pow(*, x)),6))       # Wyswietlanie informacji i obliczanie polega na szesnastkowym 
                                                            (3.14 * pow(x/2 ,  2) ) zestawu liczeb wielomianów oraz nawiasy      # Wypisywania informacji i obliczanie polega na szesnastkowym 
                                                                                                       3.14 * pow(x/2 ,  2)   + round (pow(*, x),6))      			# Pole koła o zadanym radiusie jest równy:        # Wyswietlanie informacji i obliczanie polega na szesnastkowym 
                                                                                                       3.14 * pow(x/2 ,  2)   + round (pow(*, x),6))      			# Pole koła o zadanym radiusze jest równy:        # Wyswietlanie informacji i obliczanie polega na szesnastkowym 
```        ```python    # Moduł main.py - główny plik programu (wybuch powitalny)  	 ` `` Python     def welcome():      print("Witaj w kalkulatorze matematycznim!")           import operations       if __name__ == 'main':          # Wyswietlanie pomocniczego wiadomosci i uruchamianie aplikacji    def main():  welcome()        	if _name_=='_Main' :  	        Main.run(app)      ```
