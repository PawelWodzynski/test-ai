Oto struktura plików dla projektu kalkulatora Pythonowego zawierającego trzy pliki. Każdy plik jest własnym kodem oraz moimi opisami, co on robi:
1) `operations.py` - Zawiera funkcje matematyczne (dla operacji dodawania i odejmowania). 
```python
def add(a, b):   # Funkcja do dodawania dwóch liczb czyli a oraz b.
    pass         # Zrealizuj w nastepnym bloku kodem tą rzeczy za pomocą `pass` jako domyślnego zachowanie brakujacego implementacji, to moja implementacja.
def subtract(a, b):  # Funkcje do odejmowania (brak opisu dla tego pomocniczego)... Tak samo zaimportować i zaimplementuj w nastepnym bloku kodem.
```    
2) `utils.py` - Zawiera funkcje użytkownika (dla równoległosci).  W takim razie musi być jeden, ponieważ nigdy już brakujemu implementujemy wszelko co.
```python
def parallel_execution(function1, function2):   # Funkcja umożliwia dodawanie dwóch funkcji a zwraca odpowiednik symulacji rzeczywistej pracy wielu procesorów.
    pass                                         # Zrealizuj tę sekwencję za pomocą `pass` jako domyślnego zachowanie brakujacego implementacji, to moja implementacja... Tak samo w poprzednim bloku kodem.
```  
3) Main – Ma za funckję main gdzie będą przekazywane parametry do plików matematycznych i użytkownika (biblioteki). Poniżej znajduje się podstawowe bloki kodu Markdown:
```python  # Wcześniej właściwie musiałem nazwać ten plik jako "main.py" i dodać moim opisie, czy to do zrobienia? Niestety mam problemy...
def main():   # Zrealizuj tę funkcję w bloku kodu Pythonowym na potrzeby odpalenia programu. Uwaga: Tak powinno być, ponieważ nigdy musi brakuje to zrobic...
    pass     # Zrealizuj tę sekwencję za pomocą `pass` jako domyślnego zachowanie bezpośrednio, bo wiemy że nigdy już brakemu implementacji...
```  Tak powinno być. Dobra robota musi posłuchać to jak dobrze się dowiedzie zadan lub przyjrzeniu, a teraz moja rada należy na sprób w kwestii opisanych powyżej.
